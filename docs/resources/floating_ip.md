---
page_title: "nayatel_floating_ip Resource - nayatel"
subcategory: "IaaS"
description: |-
  Allocates a Nayatel Cloud floating IP.
---

# nayatel_floating_ip (Resource)

Allocates a Nayatel Cloud floating IP.

This resource allocates an actual floating IP address that can be attached to instances.
Similar to AWS's `aws_eip` resource.

~> Due to Nayatel API limitations, an `instance_id` is required to allocate
the IP. The IP will be attached to this instance initially. Use `nayatel_floating_ip_association`
to move the IP to a different instance later.

## Example Usage

```hcl
# Allocate a floating IP (attached to instance)
resource "nayatel_floating_ip" "web" {
  instance_id = nayatel_instance.web.id
}

# Later, move it to a different instance
resource "nayatel_floating_ip_association" "web" {
  floating_ip = nayatel_floating_ip.web.ip_address
  instance_id = nayatel_instance.new_web.id
}
```

## Example Usage

```terraform
# Allocates a new floating IP and attaches it to an instance
resource "nayatel_floating_ip" "web" {
  instance_id = nayatel_instance.web.id
}

output "public_ip" {
  value = nayatel_floating_ip.web.ip_address
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `instance_id` (String) Instance ID to attach the floating IP to. Required to discover the allocated IP address.

### Read-Only

- `id` (String) Floating IP identifier
- `ip_address` (String) The allocated floating IP address
- `monthly_cost` (Number) Estimated monthly cost in Rs. for the current billing cycle
- `status` (String) Floating IP status (ACTIVE when attached, DOWN when detached)
## Import

Import is supported using the following syntax:

```shell
# Floating IPs are imported by IP address
terraform import nayatel_floating_ip.web 115.186.0.10
```
