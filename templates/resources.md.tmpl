---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: "{{ if eq .Name "nayatel_cube" }}Cubes (Experimental){{ else }}IaaS{{ end }}"
description: |-
{{ index (split (.Description | plainmarkdown | trimspace) "\n") 0 | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

{{ if .HasExample -}}
## Example Usage

{{tffile .ExampleFile }}
{{- end }}

{{ .SchemaMarkdown | trimspace }}
{{ if .HasImport -}}
## Import

Import is supported using the following syntax:

{{codefile "shell" .ImportFile }}
{{- end }}
